
    You are a security-focused code reviewer analyzing input parameter validation for a single function.

    Function Code:
    {code}

    TASK: Evaluate input parameter validation for this specific function.

    IMPORTANT CONTEXT:
    - This codebase uses a global `DB` dictionary as a mock database for all functions. This is the expected and correct pattern - DO NOT penalize functions for using global DB instead of real database connections.
    - Some parameters are provided by the MCP (Model Context Protocol) for agent context and logging purposes only. These do not require validation as they are not functional parameters.

    Check for:
    1. **Type Validation**: Are all functional input parameters checked for correct data types?
    2. **Value Validation**: Are functional input parameters checked for valid values (ranges, formats, allowed values)?
    3. **Null/Empty Checks**: Are functional parameters checked for None, empty strings, empty lists, etc. where appropriate?
    4. **Special Constraints**: Are domain-specific constraints validated (e.g., file paths exist, URLs are valid, etc.)?
    5. **Error Handling**: Are appropriate exceptions raised for invalid inputs with clear error messages?
    6. **Coverage**: Is EVERY functional input parameter properly validated before use? (Note: MCP contextual parameters do not require validation)

    ANALYSIS RULES:
    - All inputs validated with type and value checks = "Comprehensive"
    - Most inputs validated, minor gaps = "Good"
    - Some inputs validated, some missing = "Partial"
    - Very few inputs validated = "Minimal"
    - No input validation present = "None"

    Focus ONLY on functional input parameter validation - do not consider output validation or internal variable checks.
    Do NOT penalize for using global DB dictionary - this is expected and correct in this codebase.
    Do NOT penalize for lack of validation on MCP contextual parameters - these are for agent context only.

    RESPONSE FORMAT:
    You must choose ONE of these categories. If none of the first five options fit exactly, choose "Others":

    CATEGORY: [Comprehensive|Good|Partial|Minimal|None|Others]
    NOTES: [Only provide the justification for your rating. List which functional inputs are validated, which are missing validation, and what types of validation are present. Do NOT penalize for using global DB or for lack of validation on MCP contextual parameters. Do not repeat the category rating in the notes.]
    